{
  "name": "pun-api",
  "version": "1.0.0",
  "description": "PUN API",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -f script/output/*.json",
    "crawl": "node script/pun-crawl.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^24.6.0"
  }
}
